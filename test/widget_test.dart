import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App placeholder test', (tester) async {
    // Placeholder — real tests will be added in later phases
    expect(1 + 1, equals(2));
  });
}
